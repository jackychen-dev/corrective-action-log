// Firebase is optional - currently using local storage
// Uncomment and configure when ready to use Firebase

export const db = null;
export const auth = null;
export const app = null;
export default app;

